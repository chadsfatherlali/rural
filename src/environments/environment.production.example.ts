// Production environment
// IMPORTANTE: Este es un archivo de ejemplo. Copia este archivo a environment.production.ts
// y reemplaza los valores con tus claves reales de producción.
// NO COMMITTEES environment.production.ts al repositorio.

export const environment = {
  production: true,
  googleMapsApiKey: 'YOUR_PRODUCTION_API_KEY_HERE' // Replace with actual production key
};
